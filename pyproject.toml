[tool.poetry]
name = "image-logger"
version = "0.1.0"
description = ""
authors = ["sebas21312"]

[tool.poetry.dependencies]
python = "^3.8"
flask = "^3.0.0"
requests = "^2.31.0"
gunicorn = "^23.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
